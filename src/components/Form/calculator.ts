export const calculator = () => {
    
    const leaseAmount = document.getElementById('lease-amount'), 
    initialFee = document.getElementById('initial-fee'), 
    leaseTerm = document.getElementById('lease-term')
    
}
